#include <stdio.h>

int main() {
    int vetor[10];

    for (int i = 0; i < 3; i++) {
        printf("Digite um numero: ");
        scanf("%d", &vetor[i]);
    }

    

    for (int i = 0; i < 3; i++) {
        
			
	} 
        printf("\n%d", vetor[i]);
    }
            printf("\nnumero primo: posicao[%d]", i);   

   

    return 0;
}
